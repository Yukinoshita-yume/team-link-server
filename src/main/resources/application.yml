my:
  verification-code:
    code-expire-time: 300000
spring:
  application:
    name: web-app
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://150.158.136.64:3306/campus_assistant
    username: yuki
    password: Lxz@050329
  mail:
    default-encoding: UTF-8
    host: smtp.qq.com
    port: 587
    username: liu050329@qq.com
    password: pbucyhuoakerdggg
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true

mybatis:
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 8080
logging:
  charset:
    console: UTF-8
    file: UTF-8
  level:
    root: INFO
    org.springframework.jdbc: DEBUG
    org.springframework.web: DEBUG
    com.yuki.webapp.mapper: DEBUG
    com.yuki.webapp.utils: DEBUG
  file:
    name: ./logs/app.log
pagehelper:
  helper-dialect: mysql # 数据库方言
  reasonable: true # 分页参数合理化
  support-methods-arguments: true
  return-page-info: always

# 通义千问
dashscope:
  api-key: sk-a34faf314c1744bd92dc2ddc3559de58 # Qwen2 API Key
  embedding-model: text-embedding-v3
  chat-model: qwen-plus
  embedding-dim: 1024

# Qdrant 向量数据库
qdrant:
  host: localhost # 腾讯云服务器上改为服务器内网IP
  port: 6334 # gRPC端口
  collection-name: competitions

# Meilisearch
meilisearch:
  host: http://localhost:7700
  api-key: yourMasterKey123 # 与docker-compose中保持一致
  index-name: competitions
